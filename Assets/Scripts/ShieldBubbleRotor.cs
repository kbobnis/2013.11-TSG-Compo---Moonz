using UnityEngine;
using System.Collections;

public class ShieldBubbleRotor : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        transform.Rotate(3,0,0);
	}
}
